@model IEnumerable<SystemNokta.Models.Comment>

@{
    ViewBag.Title = "Index";
}

<h2>Comments</h2>


<table class="table">
   
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CommentContent)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CommentContent)
        </td>
    </tr>
}
    <tr>
        <td>
            @Html.ActionLink("Add Comment", "Add")
        </td>
    </tr>
</table>
<div>
    @Html.ActionLink("Back to List", "Back")
</div>

